import { View, Text, ScrollView } from "react-native";
import Invite from "./invite";

const InviteScreen = () => {
    return (
        <ScrollView>
            <Invite />
        </ScrollView>
    );
};
export default InviteScreen;